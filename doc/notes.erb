inline
inline with method
inline script
ujs
load everythign
classes
bundle organization and execution/instantiation
argument passing (inline, data-*)
reuse
maintainability - 1 place for the code, readability
flexability/extensibility

server side js generation/html/json


dont build html in js
duplication


Event.observe(element, ...) vs. element.observe(...)
No bind in jQ
$.each(collection, ...) vs $(collection).each(...)
$.get, $.inArray
for loop
implicit iteration

$(#) vs $$(css)[0]



Bad Ideas
* delete confirmation inline
** readability/maintainable
** flexability/extensability
 => inline method call

* add feature - create todo - do this with inline method
** ?
 => ujs
 => load everything
 => data-* # confirmation message

* add feature - error handling
** organization
 => class

* add feature - show flash message
** json